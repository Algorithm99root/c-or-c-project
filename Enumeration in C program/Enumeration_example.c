#include <stdio.h>
#include <stdlib.h>

// Enumeration In C program

enum point
{
    x = 0,
    y = 1,
    z
};

int main()
{

    printf("%d %d %d ", x, y, z);
    printf("\n");
    printf("\n");
    system("pause");
    return 0;
}